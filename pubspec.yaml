name: dialog_loader
description: An easy way to create a loader, with the dialog loader package, you can easily combine with popular state management on handling state updating, like loading, error, and success state..
version: 0.0.3
homepage: https://github.com/sud0su/dialog_loader

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
